# ----------------------------------------------------------------------
# 1. macOS (운영체제 파일)
# ----------------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# ----------------------------------------------------------------------
# 2. Editors & IDEs (개발 도구 설정)
# ----------------------------------------------------------------------
# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains (IntelliJ, GoLand, PyCharm)
.idea/
*.iml
*.iws

# Vim / Emacs / Sublime
*~
*.swp
*.swo
#*#

# ----------------------------------------------------------------------
# 3. Kubernetes & Helm (로컬 테스트 및 다운로드 파일)
# ----------------------------------------------------------------------
# Kubeconfig (절대 올리면 안 됨)
*.kubeconfig
config
.kube/

# Helm Chart 의존성 및 빌드 파일
charts/
Chart.lock
*.tgz

# ----------------------------------------------------------------------
# 4. Secrets & Logs (보안 및 잡동사니)
# ----------------------------------------------------------------------
# 환경 변수 파일
.env
.env.*

# 실수로 만든 백업 파일
*.bak
*.backup

# 로그 파일
*.log

# 키/인증서 파일 (정책 테스트용 더미 키가 아니라면 차단 추천)
*.pem
*.key
*.crt
# secrets 단어가 들어간 yaml 파일은 한번 더 확인하도록 차단 (선택사항)
*secret.yaml
*secrets.yaml